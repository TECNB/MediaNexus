# 展示动漫任务当前动作

## 背景

Task Logs 面板目前只显示日志流。日志放大后顶部空间没有展示任务当前正在执行的动作，用户需要自己从末尾日志推断状态。

## 目标

- 在 Task Logs 区域展示从最新任务日志推导出的当前动作。
- 放大日志视图同样展示当前动作。
- 保持现有 API 和 DTO 不变。

## 非目标

- 不引入全局状态或新服务端字段。
- 不重做日志面板整体设计。
- 不跑全项目校验命令。
